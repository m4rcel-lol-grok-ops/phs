# Local configuration and secrets
.env
.env.*
!.env.example

# Runtime data (SQLite database + uploaded avatars)
/data
*.db
*.db-wal
*.db-shm

# Frontend
frontend/node_modules
frontend/dist
frontend/.angular

# The Angular build is embedded at image-build time; only the placeholder that
# keeps `go build` working in a fresh checkout is tracked.
backend/internal/web/dist/*
!backend/internal/web/dist/index.html

# Go build output
backend/phs-server

.DS_Store
*.log
