# Caddy config for pornhub.singles
# Use your system caddy binary — not Docker.
# Run: caddy run --config /path/to/Caddyfile
#
# Static config only. Edit values here; no .env.

# Development: HTTP on port 80 → Docker app on 25169
:80 {
	reverse_proxy 127.0.0.1:25169
}

# Production example (HTTPS automatic via Let's Encrypt):
# pornhub.singles {
# 	reverse_proxy 127.0.0.1:25169
# }
